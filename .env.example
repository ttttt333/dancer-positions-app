# 本番（Vercel 等）でフロントだけを置くとき: Node API のオリジン（末尾スラッシュなし）
# 未設定だとブラウザは同じドメインの /api を叩く（静的ホストでは 404 HTML になる）
# VITE_API_BASE_URL=https://your-api.example.com

# 共同編集 WebSocket（任意）
# VITE_COLLAB_WS=wss://your-api.example.com
